"""Analysis over collected trial results.

Loads the flattened results, computes preference rates, the language main effect
(averaged over content and position), and the same-language position-bias baseline.
"""
