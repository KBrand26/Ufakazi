# Copy to .env (gitignored) and fill in. Inspect auto-loads .env.
# OpenRouter is the default provider path: one key, one credit pool.
OPENROUTER_API_KEY=

# Optional: only needed if you switch to native providers instead of OpenRouter.
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
